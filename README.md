# pulumi-haskell-server

### Prerequisities:
- AWS account 
- Pulumi cli installed
- Node.js installed

> 1. npm install
> 2. pulumi up
choose "yes"

copy the `url` output from the `Outputs` section

and your'e done
